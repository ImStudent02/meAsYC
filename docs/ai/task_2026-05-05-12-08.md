# Task Walkthrough: Initial Repository Setup
**Date & Time**: 2026-05-05 12:08
**Objective**: Initialize a new git repository, create a README file, and push to GitHub.

## Steps Completed:
1. **Created README.md**: Initialized the `README.md` file with the content `# meAsYC`.
2. **Git Initialization**: Ran `git init` to initialize the repository in the local directory.
3. **Staging**: Added `README.md` to the staging area with `git add README.md`.
4. **Initial Commit**: Committed the staged file with the message `first commit`.
5. **Branch Setup**: Renamed the default branch to `main` using `git branch -M main`.
6. **Remote Configuration**: Added the remote origin pointing to `https://github.com/ImStudent02/meAsYC.git`.
7. **Push to Remote**: Pushed the main branch to the GitHub repository using `git push -u origin main`.

All operations were completed successfully and the repository is now linked and synced with the remote origin.
